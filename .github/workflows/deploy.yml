name: Deploy to Hostinger

on:
  push:
    branches:
      - main
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Build Next.js Static Export
        env:
          NEXT_PUBLIC_SITE_URL: https://a4autopost.com
          NEXT_PUBLIC_DASHBOARD_URL: https://dashboard.a4autopost.com
          NEXT_PUBLIC_WORDPRESS_URL: https://wpthrust.in/
          NEXT_PUBLIC_API_URL: https://api.a4autopost.com/api/
        run: npm run build

      - name: Deploy to Hostinger via SSH/rsync
        uses: easingthemes/ssh-deploy@v5.1.0
        with:
          SSH_PRIVATE_KEY: ${{ secrets.HOSTINGER_SSH_KEY }}
          ARGS: "-avzr --delete"
          SOURCE: "out/"
          REMOTE_HOST: ${{ secrets.HOSTINGER_HOST }}
          REMOTE_PORT: ${{ secrets.HOSTINGER_PORT }}
          REMOTE_USER: ${{ secrets.HOSTINGER_USER }}
          TARGET: ${{ secrets.HOSTINGER_TARGET }}
          EXCLUDE: "/.git/, /node_modules/"
